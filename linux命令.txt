(ubuntu)Linux初始化配置：
1.sudo apt-get install ssh -y (1，2一般都已经完成安装了。)
2.sudo apt-get install vim -y
3.sudo cp /etc/apt/sources.list /etc/apt/sources.list.bak (备份源文件（这里相当于pom文件，根据源，下载对应的包）)
4.sudo vi /etc/apt/sources.list 
以下为阿里云的源：
deb http://mirrors.aliyun.com/ubuntu/ bionic main restricted universe multiverse
deb-src http://mirrors.aliyun.com/ubuntu/ bionic main restricted universe multiverse

deb http://mirrors.aliyun.com/ubuntu/ bionic-security main restricted universe multiverse
deb-src http://mirrors.aliyun.com/ubuntu/ bionic-security main restricted universe multiverse

deb http://mirrors.aliyun.com/ubuntu/ bionic-updates main restricted universe multiverse
deb-src http://mirrors.aliyun.com/ubuntu/ bionic-updates main restricted universe multiverse

deb http://mirrors.aliyun.com/ubuntu/ bionic-proposed main restricted universe multiverse
deb-src http://mirrors.aliyun.com/ubuntu/ bionic-proposed main restricted universe multiverse

deb http://mirrors.aliyun.com/ubuntu/ bionic-backports main restricted universe multiverse
deb-src http://mirrors.aliyun.com/ubuntu/ bionic-backports main restricted universe multiverse

5.sudo apt-get update (更新源)
6.sudo apt-get upgrade -y (升级所有源软件包)
7.systemctl restart systemd-logind (重启systemd-logind服务)
8.sudo apt-get install net-tools -y (安装ifconfig,可以查看网络状态)
9.sudo apt install docker.io -y (安装docker镜像)
测试
10.sudo docker --version (查看docker版本)
11.sudo docker info (查看docker详细的信息)
12.sudo vi /etc/docker/daemon.json (查看并修改docker加载源)
{
 "registry-mirrors": ["http://hub-mirror.c.163.com"]
}
13.sudo service docker restart
14.mv jdk-8u152-linux-x64.tar.gz /usr/local/lib（移动文件）
15.sudo update-alternatives --config java(切换java环境，如果有多个jdk的话)
16.sudo vi ~/.bashrc (以下为配置java环境)：

export JAVA_HOME=/usr/lib/jvm/jdk1.8.0_191 ?## 这里要注意目录要换成自己解压的jdk 目录

export JRE_HOME=${JAVA_HOME}/jre ?

export CLASSPATH=.:${JAVA_HOME}/lib:${JRE_HOME}/lib ?

export PATH=${JAVA_HOME}/bin:$PATH
17.source ~/.bashrc
18.sudo update-alternatives --install /usr/bin/java java /usr/lib/jvm/jdk1.8.0_191/bin/java 300
19.java -version(查看java版本)
20.systemctl start docker
21.查看状态：systemctl status docker
22.docker pull minio/minio (安装minio)
23.docker run -p 9000:9000 --name minio-iot -v /mnt/data:/data -v /mnt/config:/root/.minio minio/minio server /data
23.查找这个目录是哪个应用mount 过来： mount -l |more
24.查找文件：find -name *.xlsx
25.-- 备注： 运行安装已经下在的minio : 9000：9000 第一个是外部接口，第二个是内部接口， server 安装到 data
26.docker run -p 9000:9000 minio/minio server /data
27.-- tmus 可以共享命令
28.查看端口：ps -aux |grep  docker
29.检查docker安装：netstat -an |more docker
30.dpkg --list|grep mysql
31.sudo apt-get remove mysql-common（这个为例子，可以根据list中删除）
32.dpkg -l|grep ^rc|awk '{print$2}'|sudo xargs dpkg -P(清除残留痕迹数据(mysql))
33.linux 中的保存，:wq保存且退出、 q!不保存强行退出、x保存退出、
34.ls / ls -a / ls -al
35. 
set JAVA_HOME=/usr/lib/jvm/java-6-sun-1.6.0.15
export JAVA_HOME
set PATH=$JAVA_HOME/bin:$PATH
export PATH
set CLASSPATH=.:$JAVA_HOME/lib/dt.jar:$JAVA_HOME/lib/tools.jar
export CLASSPATH
格式：set (需要加的变量名)name=路径		export 变量名name
36.例如：
drwxr-xr-x 2 root root 4096 Jan 16 17:24 9a7fe28b-2da4-4887-9d6b-39d6f75170b9
-rw-r--r-- 1 root root  147 Jan 16 17:24 data-usage
d 为文件
- 为文件夹
37.docker exec -it **** 进入docker